[models]
codex_version = "5.6"
claude_tiers = ["haiku", "sonnet", "opus", "fable"]

[ignore]
globs = ["skills-archive/**", "backups*/**", "plugins/**", "projects/*/**", "file-history/**", "session-env/**", "shell-snapshots/**", "paste-cache/**", "cache/**", "security/**", "tasks/**", "jobs/**", "history.jsonl", "*.jsonl"]

[thresholds]
agent_max_lines = 60

[repos]
roots = []

[codex]
agents_md = "/Users/jfierro/.codex/AGENTS.md"
exclude_sections = ["Model Routing (Sub-Agents)"]
exclude_bullets = ["Use `planner` for feature planning", "memory & auto-memory"]
