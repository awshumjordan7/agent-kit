# Terminal module

The terminal module installs the included Ghostty configuration on macOS. Existing files are backed up before
replacement. Other platforms skip the module. A Windows Terminal version is deferred until native Windows support.
