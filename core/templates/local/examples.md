# Local settings examples

Add personal notification commands or webhook hooks to `settings.fragment.json`. For example, a command hook can run `afplay <sound-file>` on macOS, and an HTTP hook can point at a machine-local URL such as `http://127.0.0.1:<port>/hook/<private-id>`.
